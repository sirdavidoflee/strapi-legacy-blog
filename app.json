{
  "name": "Strapi",
  "description": "Strapi Starter blog",
  "keywords": ["strapi", "heroku", "starter", "blog"],
  "repository": "https://github.com/strapi/strapi-starter-blog",
  "env": {
    "AWS_BUCKET_NAME": {
      "description": "The name of your AWS bucket",
      "required": true
    },
    "AWS_BUCKET_REGION": {
      "description": "The AWS region your bucket is in",
      "required": true
    },
    "AWS_ACCESS_KEY_ID": {
      "description": "The 'API Key' of your AWS account",
      "required": true
    },
    "AWS_ACCESS_SECRET": {
      "description": "The 'API Secret' of your AWS account",
      "required": true
    }
  },
  "addons": ["heroku-postgresql:hobby-dev"],
  "image": "heroku/nodejs",
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ]
}
